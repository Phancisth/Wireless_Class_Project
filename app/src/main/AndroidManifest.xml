<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wireless_class_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Database">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".QuestionareResult" />
        <activity android:name=".Preference" />
        <activity android:name=".SignUp" />
        <activity android:name=".Questionare" />
        <activity android:name=".GradeInput" />
        <activity android:name=".Competency" />
        <activity android:name=".HomePage" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>